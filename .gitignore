# Cache et Logs Généraux
*.log
logs
*.log.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
pid.[0-9]*.json
*.pid
*.seed
*.pid.lock
lib-cov
coverage
*.lcov

# Répertoires de dépendances
node_modules/
jspm_packages/
web_modules/

# Fichiers d'environnement locaux (IMPORTANT: Ne jamais commiter les secrets !)
.env
.env.local
.env.*.local
# Inclure un fichier d'exemple si nécessaire, mais ignorer les fichiers locaux :
# !.env.example

# Fichiers système spécifiques à l'OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Fichiers temporaires
*.tmp
*.swp
*~

# --- Backend: Java / Spring Boot (Gradle & Maven) ---

# Compilé Java et Output
*.class
*.jar
*.war
*.ear
hs_err_pid*

# Build Tools
# Gradle
.gradle/
build/
*/build/
!gradle/wrapper/gradle-wrapper.jar
# Ne pas ignorer le wrapper Gradle

# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar # Ne pas ignorer le wrapper Maven

# --- Frontend: Node.js / React Native / Expo ---

# Logs (déjà couverts en partie par la section générale)

# Dépendances (déjà couvert par node_modules/)

# Fichiers Expo locaux / Cache
.expo/
.expo-shared/
web-build/
# Attention: ne pas ignorer 'assets' ou 'app.json', etc.

# Fichiers de build natif
android/
ios/
# Si tu éjectes d'Expo ou si tu génères des builds natifs localement non tracés
# Ou plus spécifiquement :
# */android/app/build/
# */ios/build/

# Fichiers de build spécifiques (peuvent être générés localement)
*.apk
*.aab
*.ipa
*.ipr
*.iws
*.iml

# Cache Metro Bundler
.metro-cache

# Cache Jest
/coverage

# Autres
.idea/
*.iml
*.project
*.classpath
*.cproject
.settings/

candiflow-ui/app-example

# --- IDE & Éditeurs ---

# IntelliJ / JetBrains IDEs
.idea/
*.iml
*.iws
modules.xml # Ancien format
# Garder potentiellement partagés : .idea/misc.xml, .idea/codeStyles/, .idea/externalDependencies.xml
# Donc, on peut ajouter des exceptions si besoin, ex:
# !.idea/misc.xml

# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
# Ajuste ces exceptions selon ce que tu veux partager avec d'autres (ou ton futur toi)

# NetBeans
/nbproject/private/
/build/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

# Eclipse
.settings/
.project
.classpath
bin/
tmp/
.metadata

# --- Docker ---

# Ignorer les fichiers d'override locaux (si utilisés)
docker-compose.override.yml
docker-compose.*.yml # Potentiellement, si tu as des fichiers locaux type docker-compose.dev.yml non partagés

# Fichiers liés aux secrets Docker (si stockés localement et non commités)
*.key
*.pem
secrets/
# Attention: Utilise les secrets Docker ou des variables d'environnement pour gérer les vrais secrets

# Cache de build Docker (géré par .dockerignore, mais peut être listé ici par sécurité)
# .docker/build-cache/

# --- Autres ---

# Fichiers de configuration locale divers
config.local.js
local.settings.json
/candiflow-api/.factorypath
